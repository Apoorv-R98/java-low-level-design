package com.expense;

import com.expense.controller.CommandDispatcher;
import com.expense.controller.commands.Command;
import com.expense.controller.commands.ExitCommand;
import com.expense.exceptions.InvalidSplitException;
import com.expense.exceptions.UserNotFoundException;
import com.expense.exceptions.DuplicateUserException;
import com.expense.services.ExpenseService;
import com.expense.services.LedgerService;

import java.util.Scanner;

public class ExpenseSharingApp {

    private static void printBanner() {
        System.out.println("=======================================");
        System.out.println("        Expense Sharing System");
        System.out.println("=======================================");
        System.out.println("Type HELP to see available commands.");
        System.out.println();
    }

    public static void main(String[] args) {
        LedgerService ledgerService = new LedgerService();
        ExpenseService expenseService = new ExpenseService(ledgerService);
        CommandDispatcher dispatcher = new CommandDispatcher(expenseService);

        printBanner();

        try (Scanner scanner = new Scanner(System.in)) {
            boolean running = true;
            while (running) {
                if (!scanner.hasNextLine()) break;
                String line = scanner.nextLine();
                if (line == null) break;
                line = line.trim();
                if (line.isEmpty()) continue;

                try {
                    Command command = dispatcher.parse(line);
                    command.execute();
                    if (command instanceof ExitCommand) {
                        running = false;
                    }
                } catch (Exception ex) {
                    System.out.println("Error: " + ex.getMessage());
                }
            }
        }





























































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































        09c
    }
}
